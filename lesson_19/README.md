# Лекція 19: Жадібні алгоритми (Greedy Algorithms)

## Зміст лекції

1. [Теорія жадібних алгоритмів](#теорія)
2. [Приклад 1: Задача про монети](#приклад-1-задача-про-монети)
3. [Приклад 2: Задача про рюкзак (0/1 Knapsack)](#приклад-2-задача-про-рюкзак)
4. [Приклад 3: Планування завдань](#приклад-3-планування-завдань)
5. [Приклад 4: Вибір активностей](#приклад-4-вибір-активностей)
6. [Приклад 5: Алгоритм Гаффмана](#приклад-5-алгоритм-гаффмана)

---

## Теорія

**Жадібний алгоритм** — це підхід до розв'язання задач оптимізації, який на кожному кроці приймає локально оптимальне рішення, сподіваючись отримати глобально оптимальне рішення.

### Принцип роботи:

1. Робимо вибір, що виглядає найкращим **прямо зараз**
2. Переходимо до підзадачі, що залишилася
3. Повторюємо до завершення

### ⚠️ Важливо:

- Жадібні алгоритми **НЕ ЗАВЖДИ** дають оптимальне рішення
- Працюють оптимально тільки для певних класів задач
- Зазвичай простіші та швидші за динамічне програмування

---

## Приклад 1: Задача про монети

**Файл:** `coin_greed.py`

### Постановка задачі

Видати суму найменшою кількістю монет номіналами: 50, 25, 10, 5, 2, 1.

### Жадібна стратегія

Завжди вибирати монету найбільшого номіналу, що не перевищує залишок.

### Складність

- **Часова:** O(n), де n — кількість номіналів
- **Просторова:** O(n) для зберігання результату

### Приклад виконання

```python
# Сума: 87
# Результат: {50: 1, 25: 1, 10: 1, 2: 1}
# Всього монет: 4
```

### Чи оптимальний?

✅ **ТАК** для стандартних монетних систем (гривні, долари тощо).

---

## Приклад 2: Задача про рюкзак

### 2.1 Брутфорс підхід

**Файл:** `backpack_bruteforce.py`

#### Алгоритм

Рекурсивно розглядаємо **всі можливі комбінації** предметів:

- Для кожного предмета: взяти або не взяти
- Вибираємо варіант з максимальною вартістю

#### Складність

- **Часова:** O(2^n) — експоненційна
- **Просторова:** O(n) — глибина рекурсії

#### Переваги / Недоліки

- ✅ Завжди знаходить оптимальне рішення
- ❌ Дуже повільний для великих n (n > 20)

---

### 2.2 Жадібний підхід

**Файл:** `backpack_greed.py`

#### Жадібна стратегія

Сортуємо предмети за **ефективністю** (value/weight) і беремо найефективніші, що влазять.

#### Складність

- **Часова:** O(n log n) — через сортування
- **Просторова:** O(1)

#### Переваги / Недоліки

- ✅ Швидкий
- ❌ **НЕ гарантує оптимального рішення** для 0/1 knapsack
- ⚠️ Працює оптимально тільки для fractional knapsack (можна брати частини)

#### Приклад неоптимальності

```
Предмети: (10, 60), (20, 100), (30, 120)
Capacity: 50

Жадібний (за ratio 6.0, 5.0, 4.0):
  - Бере (10, 60) → залишок 40
  - Бере (20, 100) → залишок 20
  - (30, 120) не влазить
  Результат: 160

Оптимальне (брутфорс/мемоїзація):
  - (20, 100) + (30, 120) = 220 ✓

Жадібний дав 160 замість 220 — на 27% гірше!
```

---

### 2.3 Мемоїзація (Динамічне програмування)

**Файл:** `backpack_memo.py`

#### Алгоритм

Рекурсія + кешування результатів для пар `(n, W)`:

- Кожен унікальний стан обчислюється лише **один раз**
- Уникаємо повторних обчислень

#### Складність

- **Часова:** O(n × W), де W — місткість рюкзака
  - Якщо W константа → O(n)
- **Просторова:** O(n × W) для мемо + O(n) для рекурсії

#### Переваги / Недоліки

- ✅ Завжди оптимальне рішення
- ✅ Набагато швидше за брутфорс
- ⚠️ Потребує додаткової пам'яті

#### Порівняння підходів

| Підхід     | Складність | Оптимальність   | Коли використовувати |
| ---------- | ---------- | --------------- | -------------------- |
| Брутфорс   | O(2^n)     | ✅ Так          | Дуже малі n (n < 20) |
| Жадібний   | O(n log n) | ❌ Ні (для 0/1) | Fractional knapsack  |
| Мемоїзація | O(n × W)   | ✅ Так          | **Рекомендовано**    |

---

## Приклад 3: Планування завдань

**Файл:** `task_scheduling.py`

### Постановка задачі

Маємо N завдань з різним часом виконання. Треба визначити порядок виконання.

### Стратегія 1: Мінімізація середнього часу очікування

#### Жадібна стратегія

**Виконуємо найкоротше завдання спершу** (Shortest Job First).

#### Чому працює?

Перше завдання входить у кумулятивну суму N разів → якщо воно найкоротше, середній час мінімальний.

#### Приклад

```
Завдання: 10h, 5h, 11h, 3h

Без оптимізації: 10 + 15 + 26 + 29 = 80 / 4 = 20h
З оптимізацією:   3 +  8 + 18 + 29 = 58 / 4 = 14.75h

Виграш: 26% швидше!
```

#### Складність

- **Часова:** O(n log n) — сортування

---

### Стратегія 2: Врахування пріоритетів

#### Жадібна стратегія

Мінімізуємо **зважений середній час**:

1. Розраховуємо ratio = time / weight для кожного завдання
2. Сортуємо за **зростанням ratio**
3. Виконуємо в цьому порядку

#### Логіка

- Менший ratio → більший пріоритет відносно часу
- Завдання (11h, weight=50): ratio=0.22 — дуже приоритетне
- Завдання (10h, weight=20): ratio=0.5 — менш приоритетне

#### Складність

- **Часова:** O(n log n)

---

## Приклад 4: Вибір активностей

**Файл:** `activity_selection.py`

### Постановка задачі

Дано N активностей з часом початку `start` та закінчення `finish`.  
**Мета:** Вибрати максимальну кількість непересічних активностей.

### Приклад

```
Активності: [(1,4), (3,5), (0,6), (5,7), (8,9), (5,9)]
Оптимальний вибір: (1,4), (5,7), (8,9) → 3 активності
```

### Жадібна стратегія

1. Сортуємо за часом **закінчення** (finish time)
2. Завжди вибираємо активність, що закінчується найраніше
3. Пропускаємо всі, що конфліктують з нею

### Чому працює?

Вибираючи активність, що закінчується раніше, залишаємо більше простору для наступних.

### Складність

- **Часова:** O(n log n) — сортування
- **Просторова:** O(1)

### Чи оптимальний?

✅ **ТАК**. Це один з класичних прикладів, де жадібний алгоритм дає оптимальне рішення.

---

## Приклад 5: Алгоритм Гаффмана

**Файли:**

- `huffman_coding.py` — базове кодування (генерація дерева і кодів)
- `huffman_coding_release.py` — повна реалізація зі стисненням/розпакуванням

### Постановка задачі

Ефективне стиснення тексту шляхом кодування символів бітовими послідовностями різної довжини.

### Ідея

- Частіші символи → коротші коди
- Рідкісні символи → довші коди

### Алгоритм (жадібна стратегія)

1. Підрахувати частоти символів у тексті
2. Створити min-heap з вузлів (символ, частота)
3. **Жадібно** об'єднувати два найрідкісніші вузли:
   - Витягти 2 мінімальні вузли
   - Створити новий вузол з сумарною частотою
   - Додати назад у heap
4. Повторювати до одного кореня
5. Генерувати коди з дерева: ліворуч = "0", праворуч = "1"

### Приклад

```
Текст: "abracadabra simsalabim"

Коди Гаффмана:
a: "11"       (найчастіший → найкоротший код)
b: "100"
i: "000"
m: "001"
r: "1011"
s: "1010"
пробіл: "0100"
l: "0101"
d: "0110"
c: "0111"

Оригінальний розмір: 176 біт (22 символи × 8 біт)
Розмір після стиснення: 67 біт
Коефіцієнт стиснення: 2.63x
```

### Складність

- **Часова:** O(n log n), де n — кількість символів
- **Просторова:** O(n)

### Чи оптимальний?

✅ **ТАК**. Алгоритм Гаффмана дає **оптимальне префіксне кодування**.

---

## Загальні висновки

### Коли жадібні алгоритми працюють оптимально?

1. **Задача має властивість жадібного вибору** — локально оптимальний вибір веде до глобального оптимуму
2. **Оптимальна підструктура** — оптимальне рішення містить оптимальні рішення підзадач

### Приклади з лекції:

| Задача              | Жадібний оптимальний? | Складність |
| ------------------- | --------------------- | ---------- |
| Монети (стандартні) | ✅ Так                | O(n)       |
| 0/1 Knapsack        | ❌ Ні                 | O(n log n) |
| Fractional Knapsack | ✅ Так                | O(n log n) |
| Task Scheduling     | ✅ Так                | O(n log n) |
| Activity Selection  | ✅ Так                | O(n log n) |
| Huffman Coding      | ✅ Так                | O(n log n) |

### Коли НЕ використовувати жадібні алгоритми?

- Задача про рюкзак 0/1 → використовуй динамічне програмування
- Задачі з складними обмеженнями → можливо потрібен перебір або DP
- Коли потрібна **гарантія оптимальності** → перевір, чи має задача властивість жадібного вибору

---

## Структура файлів

```
lesson_19/
├── README.md                      # Цей файл
├── coin_greed.py                  # Задача про монети
├── backpack_bruteforce.py         # Рюкзак: брутфорс O(2^n)
├── backpack_greed.py              # Рюкзак: жадібний O(n log n)
├── backpack_memo.py               # Рюкзак: мемоїзація O(n×W)
├── task_scheduling.py             # Планування завдань
├── activity_selection.py          # Вибір активностей
├── huffman_coding.py              # Алгоритм Гаффмана (базове кодування)
└── huffman_coding_release.py      # Алгоритм Гаффмана (повна реалізація)
```

---

## Запуск прикладів

```bash
# Монети
python coin_greed.py

# Рюкзак (всі варіанти)
python backpack_bruteforce.py
python backpack_greed.py
python backpack_memo.py

# Планування завдань
python task_scheduling.py

# Вибір активностей
python activity_selection.py

# Алгоритм Гаффмана (базове кодування)
python huffman_coding.py

# Алгоритм Гаффмана (повна реалізація)
python huffman_coding_release.py
```

---

## Ключові поняття

- **Жадібний вибір (Greedy Choice)** — завжди робимо локально оптимальний вибір
- **Оптимальна підструктура** — оптимальне рішення складається з оптимальних рішень підзадач
- **Мемоїзація** — кешування результатів для уникнення повторних обчислень
- **Динамічне програмування** — розв'язання задач через розбиття на підзадачі з кешуванням
- **NP-задачі** — задачі, для яких не відомо поліноміального рішення

---

**Успішного навчання! 🚀**
