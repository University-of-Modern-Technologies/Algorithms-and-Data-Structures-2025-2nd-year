# Lesson 17: Купи (Heaps) - Навчальний проєкт

Цей проєкт демонструє роботу з купами (heap) в Python, використовуючи модуль `heapq`.

## 📚 Порядок вивчення матеріалу

Рекомендуємо проходити файли в такому порядку - від простого до складного:

---

### 1️⃣ **main.py** - Базові операції з купами

**Що робить:**

- Демонстрація створення min heap та max heap
- Основні операції: `heapify`, `heappush`, `heappop`
- Комбіновані операції: `heappushpop`, `heapreplace`
- Пошук N найменших/найбільших: `nsmallest`, `nlargest`

**Чому починати з цього:**
Закладає фундамент - показує базові операції без зайвої складності. Студенти одразу бачать результати і розуміють як працює купа.

**Запуск:**

```bash
python main.py
```

---

### 2️⃣ **service.py + app.py** - Черга з пріоритетами

**Що робить:**

- `service.py`: власна реалізація `PriorityQueue` поверх heapq
- `app.py`: практичний приклад - обробка завдань конвертації зображень

**Чому другим:**
Показує як створити власну структуру даних і застосувати її до реальної задачі. Зрозуміла аналогія - завдання з різними пріоритетами.

**Запуск:**

```bash
python app.py
```

---

### 3️⃣ **sort.py** - Heap Sort

**Що робить:**

- Реалізація алгоритму сортування купою (heap sort)
- Два підходи: через знак та через реверс результату
- Демонстрація складності O(n log n)

**Чому третім:**
Після практичного застосування студенти готові побачити алгоритм сортування. Показує ефективність купи для сортування.

**Запуск:**

```bash
python sort.py
```

---

### 4️⃣ **median.py** - Пошук медіани в потоці даних

**Що робить:**

- Використовує **ДВІ купи одночасно** (max heap + min heap)
- Знаходить медіану за O(1), додає числа за O(log n)
- Два практичні приклади:
  - Моніторинг медіанної ціни акцій
  - Відстеження медіанного часу відгуку сервера

**Чому четвертим:**
Продвинутий приклад - показує як комбінувати дві купи для вирішення нестандартної задачі. Демонструє потужність структури даних.

**Запуск:**

```bash
python median.py
```

**Додатково:** `median_explanation.md` - детальне пояснення алгоритму з двох куп

---

### 5️⃣ **game.py** - Ігрові події з пріоритетами

**Що робить:**

- Симуляція ігрових подій з урахуванням удачі гравця
- Показує як події можуть змінювати свій пріоритет динамічно
- Використання класів та випадковості

**Чому п'ятим:**
Більш творчий та складний приклад. Підготовляє до розуміння користувацьких об'єктів в купі.

**Запуск:**

```bash
python game.py
```

---

### 6️⃣ **custom_objects.py** - Користувацькі об'єкти в купі

**Що робить:**

- Демонстрація `@dataclass(order=True)` для порівняння об'єктів
- Використання `field(compare=False)` для виключення полів
- Три практичні приклади:
  - **Task**: менеджер завдань з пріоритетами
  - **Patient**: система сортування пацієнтів швидкої допомоги
  - **Event**: черга ігрових подій за часом

**Чому останнім:**
Показує як працювати зі складними об'єктами, а не просто з числами. Демонструє гнучкість dataclass та найскладніші сценарії.

**Запуск:**

```bash
python custom_objects.py
```

**Додатково:** `custom_objects_explanation.md` - детальне пояснення роботи з об'єктами

---

## 🎯 Ключові концепції

### Min Heap vs Max Heap

- Python має тільки **min heap** (найменший елемент на вершині)
- Max heap робимо через **множення на -1**

### Складність операцій

| Операція        | Складність | Опис                    |
| --------------- | ---------- | ----------------------- |
| `heapify()`     | O(n)       | Створення купи з списку |
| `heappush()`    | O(log n)   | Додавання елемента      |
| `heappop()`     | O(log n)   | Видалення мінімуму      |
| `heap[0]`       | O(1)       | Читання мінімуму        |
| `heappushpop()` | O(log n)   | Додати і видалити       |
| `nsmallest(k)`  | O(n log k) | K найменших             |

### Коли використовувати купи?

- ✅ Черги з пріоритетами
- ✅ Пошук K найменших/найбільших елементів
- ✅ Heap sort (сортування)
- ✅ Медіана в потоці даних
- ✅ Планування завдань
- ✅ Алгоритм Дейкстри (найкоротший шлях)

---

## 📖 Додаткові матеріали

### Файли з поясненнями (Markdown)

- **median_explanation.md** - детальний розбір алгоритму MedianFinder
- **custom_objects_explanation.md** - як працювати з об'єктами в купі

### Порівняння з іншими структурами

| Задача           | Купа       | Відсортований список | Звичайний список |
| ---------------- | ---------- | -------------------- | ---------------- |
| Знайти мінімум   | O(1)       | O(1)                 | O(n)             |
| Видалити мінімум | O(log n)   | O(n)                 | O(n)             |
| Додати елемент   | O(log n)   | O(n)                 | O(1)             |
| Сортування       | O(n log n) | -                    | O(n log n)       |

---

## 🚀 Швидкий старт

1. **Вивчити основи:**

   ```bash
   python main.py
   ```

2. **Практичний приклад:**

   ```bash
   python app.py
   ```

3. **Побачити сортування:**

   ```bash
   python sort.py
   ```

4. **Складніші приклади:**
   ```bash
   python median.py
   python game.py
   python custom_objects.py
   ```

---

## 💡 Завдання для практики

1. Реалізуйте Max Heap клас вручну (без heapq)
2. Створіть систему планування завдань з дедлайнами
3. Реалізуйте алгоритм злиття K відсортованих списків
4. Створіть систему рейтингу гравців в реальному часі

---

## 📝 Примітки для викладача

- Починайте з `main.py` - там детальні коментарі до кожної операції
- Використовуйте `app.py` для демонстрації реальних застосувань
- `median.py` - для сильних студентів або бонусний матеріал
- MD файли можна давати як домашнє читання

---

## 🔗 Корисні посилання

- [Python heapq документація](https://docs.python.org/3/library/heapq.html)
- [Dataclasses в Python](https://docs.python.org/3/library/dataclasses.html)
- [Heap (структура даних) - Wikipedia](<https://uk.wikipedia.org/wiki/Купа_(структура_даних)>)

---

**Автор:** Навчальний проєкт для курсу "Алгоритми і структури даних"  
**Мова:** Python 3.x  
**Тема:** Купи (Heaps) та їх застосування
