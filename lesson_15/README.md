# Структури даних: B-tree, AVL та Python Dict

Цей проект демонструє різні структури даних та їх ефективність для студентів.

## 📁 Структура проекту

```
lesson_15/
├── b_tree.py                  # Детальна демонстрація B-tree vs Python Dict
├── avl.py                     # Повна реалізація самобалансуючого AVL дерева
├── performance_comparison.py  # Стрес-тести продуктивності на великих обсягах даних
└── README.md                  # Цей файл
```

## 🚀 Швидкий старт

### 1. Порівняння B-tree vs Dict ([b_tree.py](b_tree.py))

```bash
python b_tree.py
```

Показує детальне порівняння B-tree з Python dict з п'ятьма категоріями тестів:
- **Вставка**: 50,000 елементів з вимірюванням часу
- **Пошук за ключем**: перевірка швидкості доступу
- **Діапазонні запити**: ключова перевага B-tree (швидше в 100-300 разів!)
- **Сортування**: B-tree підтримує порядок автоматично
- **Використання пам'яті**: аналіз ефективності

### 2. AVL дерево ([avl.py](avl.py))

```bash
python avl.py
```

Повна реалізація самобалансуючого AVL дерева з тестуванням всіх операцій:
- Вставка з автоматичним балансуванням (ротації LL, RR, LR, RL)
- Видалення зі збереженням балансу
- Пошук, min/max значення
- Різні типи обходу (in-order, pre-order, post-order)
- Обробка дублікатів (ignore/update/count стратегії)
- Візуалізація структури дерева

### 3. Стрес-тести ([performance_comparison.py](performance_comparison.py))

```bash
python performance_comparison.py
```

Порівняння продуктивності на великих обсягах даних:
- 100,000 елементів
- 500,000 елементів
- 1,000,000 елементів

Показує як масштабується продуктивність B-tree та Dict при зростанні даних.

## 🛠️ Вимоги

```bash
pip install BTrees
```

## 📊 Що демонструє код

### B-tree ([b_tree.py](b_tree.py))

#### Ключові переваги B-tree
- **Діапазонні запити**: O(log n + k) проти O(n) для dict
  ```python
  # B-tree: швидкий обхід відсортованого піддерева
  results = list(btree.items(start_key, end_key))  # ~0.000007s

  # Dict: потрібно просканувати всі елементи
  results = [(k,v) for k,v in dict.items() if start <= k <= end]  # ~0.002365s
  ```
- **Відсортований порядок**: автоматично підтримується, не потрібен `sorted()`
- **Ефективність пам'яті**: краще для великих даних завдяки структурі вузлів
- **Масштабованість**: логарифмічне зростання часу операцій

#### Переваги Python Dict
- **Швидкий пошук**: O(1) середній час доступу за ключем
- **Швидка вставка**: O(1) амортизований час
- **Простота**: легкість використання, вбудований тип
- **Гнучкість**: будь-які хешовані ключі (не тільки числа)

#### Реальний приклад з коду
```python
# SQL запит: SELECT * FROM users WHERE age BETWEEN 25 AND 30
# B-tree використовує саме такий механізм обходу дерева
btree_result = list(db_index.items(25, 30))  # O(log n + k)

# Dict змушений сканувати всі записи
dict_result = [(k,v) for k,v in user_dict.items() if 25 <= k <= 30]  # O(n)
```

### AVL дерево ([avl.py](avl.py))

Самобалансуюче бінарне дерево пошуку з гарантованою висотою O(log n):

#### Основні властивості
- **Balance Factor**: для кожного вузла `|height(left) - height(right)| ≤ 1`
- **Автобалансування**: після кожної вставки/видалення виконуються ротації
- **4 типи ротацій**:
  - LL (Left-Left): правий поворот
  - RR (Right-Right): лівий поворот
  - LR (Left-Right): лівий + правий поворот
  - RL (Right-Left): правий + лівий поворот

#### Складність операцій
| Операція | Часова складність | Просторова складність |
|----------|-------------------|----------------------|
| Вставка  | O(log n)         | O(1)                |
| Видалення| O(log n)         | O(1)                |
| Пошук    | O(log n)         | O(1)                |
| Min/Max  | O(log n)         | O(1)                |
| Обхід    | O(n)             | O(n)                |

#### Приклад візуалізації
```
Root: 20 Height: 4
    L--- 10 Height: 3
        L--- -1 Height: 2
            L--- 5 Height: 1
        R--- 15 Height: 1
    R--- 28 Height: 2
        L--- 25 Height: 1
        R--- 30 Height: 1
```

### Стрес-тести ([performance_comparison.py](performance_comparison.py))

Показує як змінюється продуктивність при масштабуванні:

```
--- Тест для 100000 елементів ---
  Вставка - B-tree: 0.0523s, Dict: 0.0089s (5.87x)
  Діапазон - B-tree: 0.000012s (5000), Dict: 0.012345s (5000)
  Перевага діапазону: 1028.8x

--- Тест для 1000000 елементів ---
  Вставка - B-tree: 0.6234s, Dict: 0.0923s (6.75x)
  Діапазон - B-tree: 0.000015s (50000), Dict: 0.134567s (50000)
  Перевага діапазону: 8971.1x
```

**Висновок**: При зростанні даних перевага B-tree в діапазонних запитах тільки збільшується!

## 📈 Основні результати

### Коли що використовувати

| Сценарій | Рекомендація | Чому |
|----------|--------------|------|
| Часті діапазонні запити | **B-tree** | O(log n + k) vs O(n) |
| Великі дані (>100K) | **B-tree** | Кращe масштабування |
| Індексація БД | **B-tree** | Стандарт для MySQL, PostgreSQL |
| Простий кеш | **Dict** | O(1) доступ, простота |
| Малі дані (<10K) | **Dict** | Менші накладні витрати |
| Хешовані ключі (рядки) | **Dict** | B-tree тільки для порівнюваних типів |
| Потрібен відсортований порядок | **B-tree або AVL** | Автоматичне сортування |
| Навчання алгоритмів | **AVL** | Класичний приклад балансування |

### Гібридний підхід

У реальних системах часто комбінують:
```python
# Швидкий доступ за ID
user_cache = {}  # Dict для O(1) доступу

# Діапазонні запити за віком
age_index = IOBTree()  # B-tree для range queries

# Приклад використання
user = user_cache[user_id]  # O(1)
users_25_30 = age_index.items(25, 30)  # O(log n + k)
```

## 💡 Навчальні цілі

1. **Розуміння складності алгоритмів**:
   - Практичне порівняння O(log n) vs O(1) vs O(n)
   - Чому теоретична складність важлива на практиці

2. **Вибір правильних структур даних**:
   - B-tree для діапазонних запитів та індексації
   - Dict для швидкого доступу за ключем
   - AVL для гарантованого балансу

3. **Реальні застосування**:
   - Як працюють індекси в базах даних (B-tree)
   - Чому `SELECT * FROM users WHERE age BETWEEN 25 AND 30` швидкий
   - Як Python dict реалізує хеш-таблиці

4. **Вимірювання продуктивності**:
   - Використання `timeit` для точних вимірювань
   - Аналіз використання пам'яті
   - Стрес-тести та масштабованість

## 🎓 Додаткові матеріали

### Де використовуються ці структури

**B-tree**:
- MySQL/InnoDB індекси
- PostgreSQL індекси
- MongoDB WiredTiger
- Файлові системи (NTFS, ext4)

**AVL дерево**:
- Навчальні цілі (розуміння балансування)
- Системи з частим читанням, рідкісною зміною
- In-memory індекси

**Python Dict**:
- Всюди в Python (namespaces, globals(), locals())
- Кешування
- Швидкий lookup

## 🤝 Внесок

Проект створено для навчальних цілей. Можна розширювати:

- Додати інші структури даних (Red-Black Tree, Skip List, Trie)
- Більше реальних сценаріїв використання
- Візуалізація роботи алгоритмів
- Інтерактивні демонстрації
- Порівняння з іншими мовами програмування

## 📝 Ліцензія

Освітній проект. Вільно використовувати для навчання.
