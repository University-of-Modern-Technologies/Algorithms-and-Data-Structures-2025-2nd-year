# Демонстрація активних елементів

## Що нового

Тепер у візуалізації активні елементи виділяються **червоно-помаранчевим** кольором!

### Що показує кожен алгоритм:

#### 🔵 Bubble Sort

- Червоним виділяються два елементи що **порівнюються**
- Після обміну вони залишаються виділеними
- Приклад: `[0, 1]` - порівнюємо перший і другий елементи

#### 🔵 Insertion Sort

- Червоним виділяється елемент що **вставляється**
- Також виділяється позиція куди він **переміщується**
- Приклад: `[2, 3]` - переміщуємо елемент з індексу 3 на 2

#### 🔵 Selection Sort

- Червоним виділяється **поточний мінімум**
- Та елемент що **перевіряється**
- Приклад: `[0, 4]` - перевіряємо чи елемент 4 менший за поточний мінімум 0

## Приклад виводу

```
Bubble Sort - Step 5/18 (активні: [2, 3])
```

Це означає що на кроці 5 з 18 порівнюються елементи з індексами 2 і 3.

## Технічна реалізація

Кожна функція `*_steps()` тепер повертає:

```python
[(масив, [індекси_активних]), (масив, [індекси_активних]), ...]
```

Наприклад:

```python
[
    ([5, 3, 8, 4, 2], []),        # початковий стан
    ([5, 3, 8, 4, 2], [0, 1]),    # порівнюємо індекси 0 і 1
    ([3, 5, 8, 4, 2], [0, 1]),    # обмінялись
    ...
    ([2, 3, 4, 5, 8], [])         # фінальний стан
]
```

## Зміна кольорів

Щоб змінити колір активних елементів, відредагуйте `visualizer.py`:

```python
colors[idx] = 'gold'  # золотий замість orangered
```

Рекомендовані кольори:

- `'orangered'` ✓ (за замовчуванням)
- `'crimson'` - яскраво-червоний
- `'gold'` - золотий
- `'lime'` - яскраво-зелений
- `'magenta'` - пурпурний
- `'cyan'` - блакитний

## Запуск

```bash
python main.py
# Оберіть будь-який алгоритм
# Натискайте Next і спостерігайте за активними елементами!
```
