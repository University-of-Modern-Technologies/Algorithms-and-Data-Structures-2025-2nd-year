# Динамічне програмування: від простого до складного

Навчальні приклади для пояснення принципів динамічного програмування (ДП).

---

## 📋 Порядок файлів для лекції

1. `01_approaches.py` - Фібоначчі (мемоізація vs табуляція)
2. `02_bk.py` - Біноміальні коефіцієнти
3. `03_coin.py` - Задача про монети
4. `04_kadane.py` - Алгоритм Кадейна
5. `05_longest_increasing_subsequence.py` - Найдовша зростаюча підпослідовність
6. `06_longest_common_subsequence.py` - Найдовша спільна підпослідовність
7. `07_backpack_dynamic.py` - Задача рюкзака (0/1 Knapsack)
8. `08_rod_cutting_problem.py` - Розрізання стрижня
9. `09_bf.py` - Алгоритм Беллмана-Форда
10. `10_tsp_brute_force.py` - TSP (Brute Force для порівняння)
11. `11_tsp_hk.py` - TSP (Held-Karp алгоритм з ДП)

---

## 📚 Детальний опис

### 1. Базові концепції ДП

#### `01_approaches.py` - Фібоначчі: мемоізація vs табуляція

**Що демонструє:** Два основні підходи до ДП

- **Top-Down (Мемоізація):** рекурсія + кешування результатів
- **Bottom-Up (Табуляція):** ітеративне заповнення таблиці від базових випадків

**Складність:** O(n)  
**Коли використовувати:** Вступ до ДП, розуміння різниці між підходами

```
fibonacci(10) = 55
Top-down: зберігаємо обчислені значення в словнику
Bottom-up: будуємо масив знизу вгору
```

---

#### `02_bk.py` - Біноміальні коефіцієнти C(n,k)

**Що демонструє:** ДП на основі рекурентного співвідношення

**Формула:** C(n,k) = C(n-1,k-1) + C(n-1,k)  
**Складність:** O(n·k)  
**Застосування:** Комбінаторика, трикутник Паскаля

```
C(5,2) = 10
Таблиця dp[i][j] зберігає C(i,j)
```

---

### 2. Прості задачі (одновимірний ДП)

#### `03_coin.py` - Задача про монети

**Задача:** Знайти мінімальну кількість монет для суми

**Стан ДП:** `dp[s]` = мінімум монет для суми s  
**Перехід:** `dp[s] = min(dp[s - coin] + 1)` для всіх монет  
**Складність:** O(sum·coins)

```python
coins = [50, 25, 10, 5, 2, 1]
find_min_coins(87) → {50: 1, 25: 1, 10: 1, 2: 1}
```

**Особливість:** Також відновлює які саме монети використано

---

#### `04_kadane.py` - Алгоритм Кадейна

**Задача:** Знайти підмасив з максимальною сумою

**Ідея:** Для кожної позиції вибираємо: продовжити поточний підмасив або почати новий  
**Складність:** O(n), O(1) пам'яті

```python
arr = [2, -4, 1, 9, -6, 7, -3]
max_sum = 11, підмасив = [1, 9, -6, 7]
```

**Чому ДП:** Зберігаємо `current_sum` - максимум що закінчується на поточній позиції

---

#### `05_longest_increasing_subsequence.py` - Найдовша зростаюча підпослідовність (LIS)

**Задача:** Знайти найдовшу підпослідовність де кожен наступний елемент більший за попередній

**Стан ДП:** `dp[i]` = довжина LIS що закінчується на елементі i  
**Перехід:** `dp[i] = max(dp[j] + 1)` де `arr[j] < arr[i]`  
**Складність:** O(n²)

```python
arr = [10, 22, 9, 33, 21, 50, 41, 60, 80]
LIS = [10, 22, 33, 50, 60, 80] (довжина 6)
```

**Примітка:** Є оптимізація до O(n log n) через бінарний пошук

---

### 3. Середній рівень (двовимірний ДП)

#### `06_longest_common_subsequence.py` - Найдовша спільна підпослідовність (LCS)

**Задача:** Знайти найдовшу підпослідовність спільну для двох рядків

**Стан ДП:** `dp[i][j]` = довжина LCS для s1[:i] та s2[:j]  
**Перехід:**

- Якщо `s1[i] == s2[j]`: `dp[i][j] = dp[i-1][j-1] + 1`
- Інакше: `dp[i][j] = max(dp[i-1][j], dp[i][j-1])`

**Складність:** O(m·n)

```python
s1 = "AGGTAB"
s2 = "GXTXAYB"
LCS = "GTAB" (довжина 4)
```

**Застосування:** diff утиліти, порівняння ДНК, версійний контроль

---

#### `07_backpack_dynamic.py` - Задача рюкзака (0/1 Knapsack)

**Задача:** Вибрати предмети максимальної вартості в рюкзак обмеженої ваги

**Стан ДП:** `dp[i][w]` = максимальна вартість для перших i предметів і ваги w  
**Перехід:**

```python
if weight[i] <= w:
    dp[i][w] = max(dp[i-1][w], value[i] + dp[i-1][w-weight[i]])
else:
    dp[i][w] = dp[i-1][w]
```

**Складність:** O(n·W)

```python
items: вага=[10,20,30], вартість=[60,100,120], capacity=50
Результат: 220 (предмети 2 і 3)
```

**Класика ДП:** Одна з найвідоміших задач

---

#### `08_rod_cutting_problem.py` - Розрізання стрижня

**Задача:** Розрізати стрижень щоб максимізувати прибуток

**Стан ДП:** `dp[i]` = максимальний прибуток для довжини i  
**Перехід:** `dp[i] = max(price[j] + dp[i-j-1])` для всіх j  
**Складність:** O(n²)

```python
length = 4, prices = [1, 5, 8, 9]
Оптимально: розрізати на 2+2 → прибуток 10
```

---

### 4. Складний рівень

#### `09_bf.py` - Алгоритм Беллмана-Форда

**Задача:** Найкоротший шлях в графі з можливими від'ємними вагами

**Стан ДП:** `dist[v]` = мінімальна відстань до вершини v  
**Релаксація ребер:** n-1 ітерацій  
**Складність:** O(V·E)

```python
# Релаксація
if dist[u] + weight(u→v) < dist[v]:
    dist[v] = dist[u] + weight(u→v)
```

**Переваги:**

- Працює з від'ємними вагами
- Виявляє від'ємні цикли

**Порівняння:** Дейкстра O(E log V) але тільки для позитивних ваг

---

#### `10_tsp_brute_force.py` - Задача комівояжера (Brute Force)

**Задача:** Знайти найкоротший маршрут через усі міста з поверненням

**Підхід:** Перебір усіх перестановок  
**Складність:** O(n!)

```python
cities = 5 → 5! = 120 варіантів
cities = 10 → 10! = 3,628,800 варіантів
cities = 15 → 15! = 1,307,674,368,000 варіантів
```

**Використання:** База для порівняння з ДП рішенням

---

#### `11_tsp_hk.py` - Задача комівояжера (Held-Karp - ДП з масками)

**Задача:** Та сама, але через динамічне програмування

**Стан ДП:** `dp[(підмножина_міст, останнє_місто)]` = мінімальна відстань  
**Складність:** O(n²·2ⁿ)

```python
# Для кожної підмножини S і міста j
dp[S, j] = min(dp[S\{j}, k] + dist[k][j])
```

**Порівняння складності:**

```
n=10: Brute force = 3.6M операцій, Held-Karp = 100K операцій
n=20: Brute force = 2.4×10¹⁸, Held-Karp = 400M
```

**Техніка:** Використання `frozenset` для представлення підмножин міст як ключів словника

---

## 🎯 Ключові концепції ДП

### Коли використовувати ДП?

1. **Оптимальна підструктура:** оптимум складається з оптимумів підзадач
2. **Перекриття підзадач:** одні й ті самі підзадачі обчислюються багато разів

### Підходи до розв'язання:

1. **Визначити стан** - що зберігає `dp[...]`?
2. **Базові випадки** - найпростіші значення
3. **Рекурентне співвідношення** - як обчислити `dp[i]` через попередні
4. **Порядок обчислення** - забезпечити що підзадачі вже обчислені
5. **Відновлення розв'язку** - не тільки значення, а й сам розв'язок

### Складність:

- **Час:** кількість станів × час на стан
- **Пам'ять:** кількість станів (можна оптимізувати)

---

## 📊 Порівняльна таблиця

| Задача            | Файл                                | Розмірність | Складність   | Особливість             |
| ----------------- | ----------------------------------- | ----------- | ------------ | ----------------------- |
| Фібоначчі         | `01_approaches.py`                     | 1D          | O(n)         | Мемоізація vs табуляція |
| Біноміальні коеф. | `02_bk.py`                             | 2D          | O(n·k)       | Трикутник Паскаля       |
| Монети            | `03_coin.py`                           | 1D          | O(sum·coins) | Відновлення розв'язку   |
| Кадейн            | `04_kadane.py`                         | 1D          | O(n)         | O(1) пам'яті            |
| LIS               | `05_longest_increasing_subsequence.py` | 1D          | O(n²)        | Є O(n log n) варіант    |
| LCS               | `06_longest_common_subsequence.py`     | 2D          | O(m·n)       | Diff алгоритми          |
| Рюкзак            | `07_backpack_dynamic.py`               | 2D          | O(n·W)       | Класична задача         |
| Стрижень          | `08_rod_cutting_problem.py`            | 1D          | O(n²)        | Жадібний не працює      |
| Bellman-Ford      | `09_bf.py`                             | Граф        | O(V·E)       | Від'ємні ваги           |
| TSP Brute         | `10_tsp_brute_force.py`                | -           | O(n!)        | Для порівняння          |
| TSP Held-Karp     | `11_tsp_hk.py`                         | Маски       | O(n²·2ⁿ)     | ДП на підмножинах       |

---

## 🚀 Рекомендації для вивчення

1. **Починайте з `01_approaches.py`** - зрозумійте два підходи
2. **Практикуйте одновимірні** задачі до впевненості
3. **Переходьте до двовимірних** - візуалізуйте таблиці ДП
4. **Вивчайте складні** тільки після опанування базових
5. **Відновлення розв'язку** - не менш важливе ніж знаходження значення

---

**Автор:** Навчальний курс алгоритмів  
**Рівень:** Від початкового до продвинутого
